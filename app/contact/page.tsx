import ContactPage from "../components/Contact";

export const metadata = {
  title: "Contact Us",
  description: "Reach out to Traamp for support, inquiries, or partnership opportunities.",
};

export default function Contact() {
  return (
    <div>
      <ContactPage />
    </div>
  );
}
